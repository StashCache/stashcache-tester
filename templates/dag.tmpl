# Template for the testing dag


{% for site in sites: %}
# Jobs for site {{ site }}
JOB {{ site }} {{ site }}/submit.condor
VARS {{ site }} SITE="{{ site }}"
{% endfor %}

JOB test_reduce test_reduce.submit
PARENT {% for site in sites: %}{{ site }} {% endfor %} CHILD test_reduce

